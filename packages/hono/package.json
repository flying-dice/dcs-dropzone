{
	"name": "@packages/hono",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"exports": {
		"./*": "./src/*.ts"
	},
	"scripts": {
		"biome": "biome check --write",
		"tsc": "tsc --noEmit",
		"check": "bun run biome && bun run tsc && bunx depcheck",
		"test": "bun test"
	},
	"peerDependencies": {
		"zod": ">=4",
		"hono": ">=4",
		"hono-openapi": ">=1",
		"http-status-codes": ">=2",
		"log4js": ">=6"
	}
}
