name: Publish to WinGet

on:
  release:
    types: [released]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: FlyingDice.DCSDropzone
          installers-regex: '\.exe$'
          token: ${{ secrets.WINGET_TOKEN }}
