appenders:
  console:
    type: stdout

  errorFile:
    type: file
    filename: error.log

  onlyErrors:
    type: logLevelFilter
    appender: errorFile
    level: error

categories:
  default:
    appenders: [console, onlyErrors]
    level: info
